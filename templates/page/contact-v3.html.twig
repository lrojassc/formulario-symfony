{% extends 'base.html.twig' %}

{% block title %}Formulario 3!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Formulario V3! ✅</h1>

    {# Aqui estamos creando el formulario de la forma tradicional de html
    {{ form(form) }}
    <form action="url">
        <input type="text">
        <button>Enviar</button>
    </form>
    #}

    {% form_theme form 'bootstrap_5_horizontal_layout.html.twig' %}
    {{ form_start(form, {
        'method': 'POST',
        'attr': {'novalidate': 'novalidate'}
    }) }}

    {# Aqui se puede obtener la configuracion inicial #}
    {{ form_widget(form) }}

    {# Aqui estamos creando el formulario desde html con twig #}
    {#
    {{ form_errors(form) }}

    {{ form_row(form.message, {
        'label': 'Comoentario o sugerencia',
        'attr': {'maxlength': 10}
    }) }}

    <div>
        {{ form_errors(form.email) }}
        {{ form_row(form.email) }}
    </div>
    #}

    {{ form_end(form) }}
</div>
{% endblock %}
